<Window x:Class="ControlFilas2026.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:ControlFilas2026"
    xmlns:uc="clr-namespace:ControlFilas2026.UserControls"
    mc:Ignorable="d"
    Title="Control de Filas 2026 - Banco" 
    WindowState="Maximized"
    Background="{DynamicResource BackgroundBrush}"
    WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource DividerBrush}"
                BorderThickness="0,0,0,1"
                Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Text="Control de Filas 2026" 
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryBrush}"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Tema:" 
                               Foreground="{DynamicResource TextBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"
                               FontWeight="SemiBold"/>
                    <ComboBox x:Name="ThemeComboBox"
                              Width="120"
                              VerticalAlignment="Center"
                              SelectionChanged="ThemeComboBox_SelectionChanged">
                        <ComboBoxItem Content="Claro" IsSelected="True"/>
                        <ComboBoxItem Content="Oscuro"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" x:Name="ContentGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" 
                    Background="{DynamicResource BackgroundBrush}"
                    BorderBrush="{DynamicResource DividerBrush}"
                    BorderThickness="0,0,1,0">
                <uc:AdministracionUC x:Name="AdminControl" 
                                     Visibility="Collapsed"/>
            </Border>
            
            
            <Border Grid.Column="1" 
                    Background="{DynamicResource BackgroundBrush}"
                    BorderBrush="{DynamicResource DividerBrush}"
                    BorderThickness="1,0,1,0">
                <Grid x:Name="MiddleContent">
                    <uc:LoginUC x:Name="LoginControl" 
                                HorizontalAlignment="Stretch" 
                                VerticalAlignment="Stretch"/>
                    
                    <uc:CajasUC x:Name="CajasControl" 
                                Visibility="Collapsed"/>
                </Grid>
            </Border>
            
            
            <Border Grid.Column="2" 
                    Background="{DynamicResource BackgroundBrush}"
                    BorderBrush="{DynamicResource DividerBrush}"
                    BorderThickness="1,0,0,0">
                <uc:ClienteUC x:Name="ClienteControl" 
                              Visibility="Collapsed"/>
            </Border>
        </Grid>

        <uc:AbajoUC Grid.Row="2"/>
    </Grid>
</Window>
